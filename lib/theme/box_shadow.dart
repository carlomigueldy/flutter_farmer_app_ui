import 'package:flutter/material.dart';

BoxShadow kBoxShadowPrimary = BoxShadow(
  color: Colors.black.withOpacity(0.3),
  spreadRadius: 1,
  blurRadius: 7,
  offset: Offset(0, 3), // changes position of shadow
);
